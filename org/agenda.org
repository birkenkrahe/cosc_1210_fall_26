#+TITLE: Agenda 
#+AUTHOR: Marcus Birkenkrahe 
#+SUBTITLE: COSC 1210 (CS I) FA26 | Augustana U.
#+STARTUP: overview hideblocks indent entitiespretty
#+OPTIONS: toc:nil num:nil ^:nil

* DONE W1S1: Overview
#+attr_html: :width 600px :float nil:
[[../img/Flammarion.jpg]]
Image: from Camille Flammarion's L'Atmosphère: Météorologie populaire
(1888) - "A medieval missionary tells that he has found the point
where Heaven and Earth meet.[fn:1]

- *Check-in when in class:*
  
|   | Topic                                                                   |
|---+-------------------------------------------------------------------------|
| x | Welcome & objectives                                                    |
| x | Get to know your teacher & share why you're here                        |
| x | Understand what this course is about                                    |
| x | Find out where to find stuff: Canvas, GitHub, textbook, DataCamp/Kattis |
| x | Whom to ask, formalities, and grading                                   |
| x | Weekly rhythm: in-class, exams, and at-home work                        |
| x | Setup: JupyterLite, onecompiler, python.org                             |
| x | Assignments & what's next (content still TODO in overview file)         |
|---+-------------------------------------------------------------------------|

File: See =org/1_overview.org= in GitHub

* DONE W1S2: Programming as a way of thinking
#+attr_html: :width 600px :float nil:
[[../img/breughel_babel_1563.jpg]]

Image: The (Great) Tower of Babel, by Pieter Bruegel the Elder
(c. 1563 AD)[fn:2]

- I sorted the Google Space problem out (with the help of IT): You
  should have received an invitation from me.

- I also added the link to the class photo to Canvas: please open it
  and add your name to your face so that I can learn it.

- I added a link to the Google Meet recordings in Canvas.

|   | Topic                                                         |
|---+---------------------------------------------------------------|
| x | Hello world, errors, and your first working program           |
| x | Building a tax calculator: hardcoded -> general -> visualized |
|   | Why programming?                                              |
|   | Why programming in Python?                                    |
|   | Glossary & wrap-up                                            |

* DONE W2S1: Python basics - arithmetic, expressions, precedence

|   | Block                                             | min |
|---+---------------------------------------------------+-----|
|   | Housekeeping: weekly email / Canvas notifications |   5 |
|   | Review of last week (retrieval warm-up)           |   5 |
|   | Illusions of the machine                          |   4 |
|   | Arithmetic operators                              |   5 |
|   | Expressions (identity elements, L->R, unary)      |  11 |
|   | Operator precedence: PEMDAS, floats, sqrt (show)  |  15 |
|   | PRACTICE: Formula translator (formulae 1-5)       |  20 |
|   | Debrief + summary                                 |   5 |
|---+---------------------------------------------------+-----|
|   |                                                   |  70 |
#+TBLFM: @10$3=vsum(@2..@9)

** Housekeeping

- I sent you a long (weekly) email (twice, by mistake) as an
  [[https://augie.instructure.com/courses/15635/discussion_topics/79638][announcement in Canvas]]. Did you get it/read it? You may have to
  change your Canvas settings: Account > Notifications > Announcements
  > Notify immediately.

** Review of last week

- What do you remember?

- What was worth remembering:
  #+begin_quote
  + Programming means telling a machine what to and what not to do
  + Programming notebooks (like Jupyter) are written for humans
  + Programming has a gazillion parts but only a few principles
  #+end_quote

** Python arithmetic part 1

- [X] Illusions of the machine
- [X] Arithmetic operators
- [X] Expressions
- [X] Operator precedence
- [X] *Practice:* formula translator

See: [[https://github.com/birkenkrahe/cosc_1210_fall_26/blob/main/org/3_arithmetic.org][3_arithmetic.org in GitHub]].

* DONE W2S2: Python basics - more operators, strings, values, errors and types

** Housekeeping - Code-along uploads & DataCamp

- Several of you are still missing submissions for *code-along
  files*. This will affect your *participation* grade.

- You usually have *one week* to upload a code-along file if you did not
  finish it in class, or if you missed class and need the recording.

- I check all your submissions, including the code along files. AI
  flags submissions that are not standard, unfinished or missing.

- I will review the data on the weekend and may reach out to you.

- *DataCamp*: few of you have registered with this platform. While it is
  not mandatory, you should take a look at it and use the additional
  opportunity to practice Python, esp. if you're into *data science*.

- The material of this week, for example, is covered in the first
  lesson of the *Introduction to Python* course - takes only 20 minutes.

- Once you've completed a lesson you get daily practice opportunities:
  5 minutes of Python every day will keep you immersed and make your
  learning much easier and more efficient!
  
** Review

I ask a question and you (hopefully) know the answer. You don't have
to say anything - just make sure yourself that you are on top of it.

1) What does *abstraction* in the sense of "removing detail" always cost
   you?
   #+begin_quote
   Removing detail always implies a loss of information. If you have
   less information, it is easier to fall for an illusion: each
   abstraction is the machine playing a trick that lets you get on
   with your work.
   #+end_quote

2) What are the three parts of a computer and what distinguishes them?
   #+begin_quote
   - The CPU is super-fast: access latency is about 0.3 ns (billionth)
   - The RAM is fast: access latency is about 100 ns
   - The NVM is super-slow: access latency of a hard disk is 10 ms

   If one CPU cycle was 1 second, then the RAM is a coffee break, a
   SSD is a full day, and a HDD is a year.
   #+end_quote

3) In Python, what is =-2 * 5**2 - 10=
   #+begin_src python :results output :exports both :session *Python* :python python3 
     print(-2 * 5**2 - 10) # -(2 * 25) - 10 = -60
   #+end_src

   #+RESULTS:
   : -60

4) In Python, what is =1/3= and why?
   #+begin_src python :results output :exports both :session *Python* :python python3 
     print(1/3) # appears as a float with 17 digits
     print((1/3).as_integer_ratio()[0].bit_length()) # stored on 53 bits
     print((3).as_integer_ratio()[0].bit_length()) # stored on 2 bits (2^0 + 2^1 = 11)
   #+end_src

   #+RESULTS:
   : 0.3333333333333333
   : 53
   : 2

5) What's wrong with this code?
   #+begin_example
       print 5 ** 2
   #+end_example
   #+begin_src bash :results output :exports both
     python3 -c "print 5 ** 2" 2>&1  # SyntaxError
   #+end_src

   #+RESULTS:
   :   File "<string>", line 1
   :     print 5 ** 2
   :     ^^^^^^^^^^^^
   : SyntaxError: Missing parentheses in call to 'print'. Did you mean print(...)?

** Python arithmetic part 2

- [X] More operators
- [X] Built-in functions
- [X] String operations

** Take aways

- Check out with one word: something that you take away from class
  today as
  1. something you either did not know yet, or
  2. that you perhaps know but understand better, or
  3. that you knew but liked to see and use again.

* DONE W2S3: Python basics
#+attr_html: :width 400px :float nil:
[[../img/Programming_Arithmetic_Fundamentals_Guide.png]]
** Extras

Highlighting some bonus content for this class ([[https://chat.google.com/room/AAQAeL5a2zw?cls=7][from the chat]]):
1) Spam, Spam, Spam  by Monty Python (1970)
2) JupyterLab installation notes
3) The Beauty of Programming by Linus Torvalds (2001)
4) ELIZA chatbot by Joseph Weizenbaum (1966)
5) Python resources: python.org, realpython.com, freeCodeCamp
6) Is learning to code in 2026 still important? YouTube Playlist

You will get a 1-page infographic and a video overview for this week
(link in Canvas in the 2. Programming module to Photos, Slides,
Videos).

** Review

1) ~divmod~ is a function that combines integer division and modulo
   operation: =divmod(a,b) = (a // b, a % b)=

   What is the result of =divmod(10,3)=?
   #+begin_src python :results output :exports both :session *Python* :python python3 
     print(divmod(10,3)) # 10 = 3 * 3 + 1
   #+end_src

   #+RESULTS:
   : (3, 1)

2) How would you convert 500 days to whole days and leftover hours?
   #+begin_src python :results output :exports both :session *Python* :python python3 
     print(divmod(500,24)) # how many times does 24 fit in 500
     print(f"500 days are {divmod(500,24)[0]} whole days \
     and {divmod(500,24)[1]} hours.")
   #+end_src

   #+RESULTS:
   : (20, 20)
   : 500 days are 20 whole days and 20 hours.

3) What do you need to remember about using ~round~ on decimals ending
   in 5?
   #+begin_quote
   ~round~ works from the actual stored value, not the decimal
   literal. A decimal ending in 5 is almost never a perfect tie in
   binary.
   #+end_quote
   #+begin_src python :results output :exports both :session *Python* :python python3 
     print(round(3.675,2))
   #+end_src

   #+RESULTS:
   : 3.67

4) What do you get when you run =print("Spam") * 5= ?
   #+begin_quote
   ="Spam"= because the machine executes from the left, and then you get
   a ~TypeError~ because the operands =print("Spam")= and =5=, cannot be
   multiplied (=5= is an integer, and =print("Spam")= returns "nothing"
   aka ~None~ - it only does something, namely print.
   #+end_quote

5) What's "XOR" stand for?
   #+begin_quote
     XOR is a logical operator. It is ~True~ only if its two operands
     have different truth (aka boolean) values:

     | p     | q     | p XOR q | Example                   |
     |-------+-------+---------+---------------------------|
     | True  | True  | False   | N is even and odd         |
     | True  | False | True    | N = 4: even, not odd      |
     | False | True  | True    | N = 7: odd, not even      |
     | False | False | False   | N is neither even nor odd |

     In Python, the caret (~^~) is the XOR operator for binary values.
   #+end_quote
   #+begin_src python :results output :exports both :session *Python* :python python3 
     n = 5
     print((n % 2 == 0) ^ (n % 2 == 1)) # always True for any integer n
                                        # (tautology)
   #+end_src

** Code-along lecture + Practice

- Open JupyterLite in a browser: https://jupyter.org/try-jupyter
- Code along with me
- Download your notebook's .ipynb file
- Upload the file to Canvas:
  [[https://augie.instructure.com/courses/15635/assignments/272783][Code-Along: String functions, values, types, debugging]]

** Home assignments (by Sept 11, 11:59 pm)

- Watch the video recording, upload your code along file
- Quiz 2 covering all content from this week (incl. the video)
- Programming assignment: String Art

* DONE W3S1: Variables and statements
#+attr_html: :width 200px :float nil:
[[../img/Programming_Variables_and_Statements_Guide.png]]

** Housekeeping

- Video recording in Canvas-YuJa:
  1. Review of week 2
  2. String functions and a practice problem
  3. Practice problem solution
  4. Values, types, type conversion
  5. Debugging and a gallery of Python errors

- You must upload the code-along notebook
  
- New bonus assignment "Debug the Bill Splitter"

- Infographic, slide deck, and video overview for this week

** Review: strings, values, types, and errors

From week 2, part 2 (string functions onward) - the ground quiz 2 covers.

1) Strings are called *immutable*. What does that mean, and what do the
   string methods ~upper~ and ~lower~ actually do to a string?
   #+begin_quote
   Immutable = you cannot change a character inside an existing
   string; the string object itself never changes. ~"hello".upper()~
   does not touch ="hello"= - it builds and returns a *new* string
   ="HELLO"=.
   #+end_quote

2) Why is ~len("hello")~ written differently from ~"hello".upper()~?
   #+begin_quote
   ~len~ is a *built-in function*: the string goes inside its parentheses
   as an *argument*. ~upper~ is a *string method*: it belongs to the string
   and you reach it with the *dot operator*, with empty parentheses.
   You can also call ~len~ on other other data structures, e.g. a list.
   #+end_quote
   #+begin_src python :results output :exports both :session *Python* :python python3
     print(len("hello"))      # function - argument in ()
     print("hello".upper())   # method - reached with the dot
     print(len(range(0,9)));  # calling len() on a range of numbers
     print(type(range(0,9)))
     print(len([100,"hello", [3.14,2.71]])) # calling len() on a list
   #+end_src

3) In ="Spam, " * 5 + "and Spam!"=, does the ~*~ or the ~+~ happen
   first, and what is the result?
   #+begin_src python :results output :exports both :session *Python* :python python3
     print("Spam, " * 5 + "and Spam!")   # * binds tighter than +
   #+end_src

4) What happens when you run =print(2 + " Spam " * 5)=?
   #+begin_quote
   A ~TypeError~. The ~*~ is fine (=str * int= repeats the string), but
   then ~+~ needs a string on both sides and =2= is an integer - the
   same reason ="Spam" + 5= fails.
   #+end_quote
   #+begin_src python :results output :exports both :session *Python* :python python3 
     print(str(2) + " Spam " * 5)
   #+end_src

   #+RESULTS:
   : 2 Spam  Spam  Spam  Spam  Spam
   
5) Python is *dynamically typed*, and every value has a type. Name the
   four basic types, with one value of each.
   #+begin_src python :results output :exports both :session *Python* :python python3
     print(type(2))        # int   - whole number
     print(type(33.3))     # float - number with a decimal point
     print(type("hello"))  # str   - text
     print(type(True))     # bool  - True / False
   #+end_src

   #+RESULTS:
   : <class 'int'>
   : <class 'float'>
   : <class 'str'>
   : <class 'bool'>

6) What does =int(33.9)= give? And =int(-33.9)=? Is that rounding?
   #+begin_src python :results output :exports both :session *Python* :python python3
     print(int(33.9))   # 33  - drops the fraction, does NOT round
     print(int(-33.9))  # -33 - truncates towards zero
   #+end_src

   #+RESULTS:
   : 33
   : -33

7) =int("444")= works but =int("hello")= does not. What is the
   difference?
   #+begin_quote
   =int("444")= returns =444=, an ~int~. =int("hello")= raises a ~ValueError~:
   the argument is the right *type* (a string) but its *value* cannot be
   read as a decimal number.
   #+end_quote

8) =print(str(33))= and =print(33)= put the same thing on screen. So
   what did ~str~ actually change?
   #+begin_quote
   Nothing you can see - ~str~ converts the *type* from ~int~ to ~str~.
   The type is not visible in the output; you check it with ~type~.
   #+end_quote
   #+begin_src python :results output :exports both :session *Python* :python python3
     print(str(33))         # looks like 33
     print(type(str(33)))   # ...but it is a string now
     print(type(33))        # the original is still an int
   #+end_src

   #+RESULTS:
   : 33
   : <class 'str'>
   : <class 'int'>

9) A rule of thumb: when do you get a ~ValueError~ and when a
   ~TypeError~? Could you come up with an example for each?
   #+begin_quote
   Wrong *type* for the operation -> ~TypeError~. Right type but an
   unusable *value* -> ~ValueError~.
   #+end_quote
   #+begin_src python :results none :exports both :session *Python* :python python3 
     print(2 + "2") # TypeError: unsupported operand type(s) for +: 'int' and 'str'
   #+end_src
   #+begin_src python :results output :exports both :session *Python* :python python3 
     print(int("hello")) # ValueError: invalid literal for int() with base 10: 'hello'
   #+end_src

   #+RESULTS:
   
10) Name the three families of Python errors and when each strikes.
    #+begin_quote
    - /syntax/ errors: bad grammar - caught *before* the code runs
    - /runtime/ errors: a crash *during* execution, at the failing line
      (~NameError~, ~TypeError~, ~ZeroDivisionError~, ~IndexError~, ...)
    - /semantic/ errors: it runs and finishes, but the answer is wrong -
      no message at all (use a debugger e.g. pythontutor.com)
    #+end_quote

** Variables and statements (ch. 2)

After completing this code-along lecture with practice, you can:
- Variables to store values
- Importing a library
- Expressions vs. statements
- ~print~ function
- Function arguments
- Extended eample: Pythagoras' theorem
- Home assignment: Pay stub calculator
- Bonus assignments: Circle Calculator, Change Maker

* DONE W3S2: Functions I - defining and calling functions

*Today:*
- Defining functions with ~def~
- Telling parameters from arguments
- Documenting functions

*Next assignments:*
- Pay stub calculator (submit your notebook by Sept 18)
- Quiz 3 (Variables and statements and functions I) - by Sept 18
- Bonus assignment: Change Maker (Oct 9)
- Bonus assignment: Circle Calculator (Oct 9)
* DONE W4S1: Test (30 min)

- Popquiz: 30 min, self-graded for extra credit (optional)
- Test for Midterm exam (50 min) 
- Shorter session today (50 min) because I've got to give a talk

* DONE W4S2: Functions II - functions calling functions & loops

We now move from writing standalone functions to building small
functions that call and adapt each other.

What you can do now: write one small set of functions once and reuse
it for a whole family of formatted output problems: notices, receipts,
bulletins, just by changing what's passed in (= interface).

- Calling one function from another
- Generalizing a function
- Stacking functions
- Loops with ~for~ and ~range~

** Checkout - what do you remember from today?

* DONE W4S3: Functions III - loops inside functions and functions inside loops

Four weeks in, here's the tally:

|--------------------------------------+-----|
| Class sessions so far                | 10  |
| Pages of material covered            | ~50 |
| Lines of Python code you've seen     | 568 |
| Lines of it you ran live in class    | 526 |
| Class attendance index               | 98% |
|--------------------------------------+-----|

** How to really learn this stuff

Your activities:
- Coding alongside me (Jupyter)
- Solving weekly programming problems (Canvas)
- Solving bonus problems (Canvas)
- Reading the textbook (online)
- Reading my lectures (GitHub)

You need all of these to really learn how to program, which comes down
to: building a habit (you do it routinely) and intuition (you feel it).

** How to read, how to study, how to learn programming

I'm going to make a video on this, I think, because it's important:

1) You don't learn by *grazing* (like a cow eat grass)

2) You don't learn by *gorging* (like a shark)

3) You don't learn by *gallivanting* (like a tourist)

4) You only learn by *grappling* with the material (like a wrestler)

5) How does one immerse oneself deeply (in CS)?
   #+begin_quote
   - Read the same passage (or code) again and again until it makes
     sense to you (or until you understand why it does not make sense)
   - Practice in small daily doses rather than cramming once a week --
   - Write code every day, even for ten minutes, the way you'd
     practice an instrument.
   - Struggle with a problem for a while before looking up (or asking
     AI for) the answer: the friction is where the learning happens,
     not the solution itself.
   - Produce, don't just consume -- reading someone else's code or
     watching a video feels like progress but doesn't build the same
     muscle as typing it yourself, breaking it, and fixing it.
   - Explain what you wrote to someone else, or out loud to yourself,
     or to the AI: if you can't explain it, you haven't actually
     learned it yet, no matter how cleanly it compiles.
   #+end_quote

** Review

1) Predict the output:
   #+begin_src python :results output :exports both :session *Python* :python python3
     def rule(length=24):  # need not provide an argument - rule() is OK
         print("-" * length)

     def notice(text): # must have a text argument
         rule(len(text))
         print(text)
         rule(len(text))

     #notice() # must provide an argument!
     notice(1000)
   #+end_src

   #+RESULTS:

   #+begin_quote
   TypeError: notice() missing 1 required positional argument: 'text'
   #+end_quote   

2) ~header~ is defined with no parameters. What happens when you call
   it with an argument anyway?
   #+begin_src python :results output :exports both :session *Python* :python python3
     def header():
         print("COSC 1210 - Bulletin")

     header(20) # same with header("announcement")
   #+end_src

   #+RESULTS:

   #+begin_quote
   TypeError: header() takes 0 positional arguments but 1 was given
   #+end_quote
   
3) ~rule~ expects a number for =length=. What happens if you call it
   with a string instead?
   #+begin_src python :results output :exports both :session *Python* :python python3
     def rule(length=24):  # default parameter is a number
         print("-" * length)

     rule("five")
   #+end_src

   #+RESULTS:
   : -----

   #+begin_quote
   TypeError: can't multiply sequence by non-int of type 'str'
   #+end_quote

4) What happens here, and why?
   #+begin_src python :results output :exports both :session *Python* :python python3
     range = 3
     print(range(5))
   #+end_src

   #+RESULTS:


   #+begin_quote
   =range= is an ordinary built-in function, not a keyword - assigning
   to it (=range = 3=) overwrites it with the integer =3=. The name =range=
   no longer refers to the function at all, so trying to *call* it
   fails: you can't call an =int=. Nothing here is special to =range= -
   the same thing happens to any name you reassign this way.
   #+end_quote

5) Trace the call chain and predict the output:
   #+begin_src python :results output :exports both :session *Python* :python python3
     def rule():
         print("-" * 10)

     def header():
         rule()
         print("Hi".center(10))
         rule()

     def bulletin(item):
         header()
         print("* " + item)
         rule()

     bulletin("Test")
   #+end_src

   #+RESULTS:
   : ----------
   :     Hi    
   : ----------
   : * Test
   : ----------

** Functions III: loops inside functions and functions inside loops

- the ~range~ trap
- generators
- explain why a variable made inside a function is invisible outside it
- read a traceback from the bottom up
- two practice problems

** Assignments

- Home programming assignment: Certificate Printer (easy)
- Bonus program: personal savings growth calculator (medium)
- Bonus program: build a toolkit for enzyme analysis (hard)

* W5S1: Ultimate Function Test

- [X] Late quiz policy
- [X] Code along uploads
- [X] Programming assignments
- [X] Review: Paysstub Calculator
- [X] In-class test: functions
- [ ] Review (last class)
- [ ] Conditionals and recursion

** Late quiz policy

- Quizzes state that you lose 50% for late submission.

- I have been lenient for Quiz 1 + 2 to ensure that everybody
  understands the rules.

- From Quiz 3, the policy will be applied rigorously.

- Quizzes have a deadline and will be open for another week.

** Code along uploads

- Similarly for the code alongs: I've been lenient at the start.

- But the numbers aren't great.

- Class performance index so far:

  |       Date | Code-along / assignment              | Index       |     % |
  |------------+--------------------------------------+-------------+-------|
  | 2026-09-04 | String functions (video)             | 96.5 / 126  | 76.6% |
  | 2026-09-09 | Variables and statements (final)     | 133 / 190   | 70.0% |
  | 2026-09-11 | Programming assignment 1: String Art | 75 / 105    | 71.4% |
  | 2026-09-18 | Functions, whole week (final)        | 133.5 / 228 | 58.6% |

  Only students who submitted count in the index; missing submissions
  are not included.

- There will only be code-along uploads at the end of the week for
  whatever we've done that week. The upload will be open for one week.

- There will be point reductions for incomplete sections.

- So if you're missing, or if you didn't do a practice (i.e. did not
  participate), this is going to hurt your grade.

- You have to check using the recording or my GitHub record and update
  your code along files before submitting them.

- You should know if your notebooks are incomplete. If you're unsure
  where you are in my participation ranking, ask me.

** Programming assignments

- When I ask you to submit a notebook, I want you to submit a
  notebook, and not a blob of script. Machines write blobs, humans
  write notebooks.

- Example: I asked for you to check the length of a string with
  ~len()~. That's not part of the final program but it should be part of
  your process (instead of counting the length by hand).

- The assignments are split into tasks for a reason. You should
  discipline yourself to write the programs in that same way. 

- When I ask for a write-up, I am asking for a text cell.

- When an output contains double spaces, your result should also
  contain double spaces. I only subtracted points for missing parts.

- Your future submissions will have to include a header (title,
  author, and a statement on how you used AI if you did at all).

- See my sample solution (PDF) for what I mean by "notebook
  solution". My solution also includes a few extensions:
  [[https://tinyurl.com/string-art-2][tinyurl.com/string-art-2]] (in Google Colab)

- Several of you did not submit: Use the calendar!

- The class performance overall according to AI is 71%.

- Assignments also carry a 50% penalty for late submission.
  
** Review: Paystub Calculator (home assignment)

- Detailed sample solution: [[https://tinyurl.com/paystub-calculator-solution][tinyurl.com/paystub-calculator-solution]]

** In-class test: functions

- Open the test page in Canvas
- Follow the instructions
- Upload solution to Canvas
- Time: 25 minutes

** Review (last class)

Self-check as usual.

1) What is the difference between a parameter and an argument?
   #+begin_quote
   A parameter is a name in the function definition; an argument is
   the value you pass in the call. Calling ~greet("Jim")~ works as if
   ~name = "Jim"~ ran first.
   #+end_quote

2) A function computes ~total = price * qty~ and prints it. You then
   run ~print(total)~ outside the function. What happens?
   #+begin_quote
   ~NameError~ - ~total~ is local to the function and gone once it
   has finished.
   #+end_quote

3) What values does ~i~ take in ~for i in range(3):~?
   #+begin_quote
   0, 1, 2.
   #+end_quote

4) Which line of a traceback do you read first?
   #+begin_quote
   The last one: it names the error. The lines above it show how the
   program got there.
   #+end_quote

5) What is a docstring for, and how do you read one?
   #+begin_quote
   It documents what a function does and what its parameters mean;
   ~help(f)~ prints it.
   #+end_quote


* W5S2: Viking 1: Mission Control (Conditionals & Recursion)

- [ ] Function test debrief

** Test debrief
#+attr_html: :width 400px :float nil:
#+caption: Rome: Not built in one day (2775 BC)
[[../img/roma.png]]

- If you achieved < 30%, I recommend you try the same exercise again
  without peeking. Just massage that code memory once more. See me for
  office hours any time for a Python booster.

- If you achieved 30% - 70%, try the new batch of function exercises
  (built just like the first one, slight changes) and earn bonus
  points (solutions link at the bottom of the assignment).

- If you achieved 80%-100%, you're already on top of your game, and
  all you need to do is keep going & keep me on my toes! Try some of
  the other open exercises to keep yourself interested.

* Footnotes

[fn:1]That's what we're doing all semester: Not just using tools, but
reaching past the interface to see, and eventually build, what's
operating behind it.

[fn:2]The Tower of Babel fell to a confusion of languages —
programming is the discipline of never letting that happen between you
and the machine.
